# ❌ REMOVE ReferralManager FROM XCODE

## The file was deleted, but Xcode still references it.

### **Quick Fix:**

1. Open Xcode
2. In the left sidebar, find **"Managers"** folder
3. Look for **"ReferralManager.swift"** (it will be RED/missing)
4. **Right-click** on it
5. Select **"Delete"**
6. Choose **"Remove Reference"** (NOT "Move to Trash")
7. Build again (⌘B)

### **OR via Command Line:**

I can remove it from the project file programmatically.

Should I do that for you?

