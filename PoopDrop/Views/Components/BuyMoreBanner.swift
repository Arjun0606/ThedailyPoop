import SwiftUI

struct BuyMoreBanner: View {
    @Binding var selectedTab: Int
    
    var body: some View {
        // Banner removed - no longer showing "Running low on attacks"
        EmptyView()
    }
}
